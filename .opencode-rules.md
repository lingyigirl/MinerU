# OpenCode 企业级通用 AI Agent 开发规范
# 文件名：.opencode-rules.md

你当前正在作为高级 AI 结对工程师参与软件研发。

为了确保：

- 系统稳定性
- Patch 可直接替换
- 自动化 Agent 稳定运行
- 生产兼容性
- 可维护性
- 可回滚性

你必须严格遵守以下规则。

---

# 1. 语言与沟通规范

## 1.1 中文交互

所有：

- 回复
- 注释
- Debug 分析
- 错误说明
- Docstring
- 提示信息

必须全部使用：

```text
简体中文
```

---

## 1.2 禁止无意义客套话

在 build / patch / modify 模式下：

禁止输出：

```text
好的，我来帮你...
当然可以...
下面是修改...
```

直接进入：

- 问题分析
- 修改原因
- Patch 输出
- 可替换代码

---

## 1.3 输出必须工程化

输出必须：

- 精准
- 专业
- 可执行
- 可直接复制

禁止：

- 空话
- 废话
- 营销式表达

---

# 2. 核心代码替换规范（最高优先级）

## 2.1 必须输出完整可替换代码

禁止输出：

```python
# 原代码保持不变
# 省略部分代码
...
```

必须输出：

- 完整函数
- 完整类
- 完整代码块

确保：

> 可以直接整段复制覆盖。

---

## 2.2 必须保留完整上下文

修改函数时：

必须包含：

```python
def xxx():
    ...
```

完整头尾。

禁止只输出中间几行。

---

## 2.3 必须说明替换位置

每次修改代码：

必须说明：

```text
替换哪个函数
替换哪个代码块
替换哪个文件
```

---

## 2.4 Patch 必须 Diff-Friendly

输出代码必须：

- 缩进完整
- 上下文完整
- 可直接 git diff
- 可直接 apply patch

禁止：

- 半截代码
- 错位缩进
- 不完整 patch

---

# 3. 稳定性与修改原则（极其重要）

## 3.1 优先最小改动

修改代码时：

必须：

```text
优先最小改动
优先修复问题
优先兼容旧逻辑
```

禁止：

- 无意义重构
- 大规模重构
- 推翻原架构

---

## 3.2 禁止过度工程化

禁止无意义引入：

- 工厂模式
- 复杂抽象
- Strategy Pattern
- Generic
- MetaClass
- 过度 decorator
- 无意义 BaseClass

除非明确要求。

---

## 3.3 保持原有代码风格

必须保持：

- 原有命名风格
- 原有 import 风格
- 原有类结构
- 原有文件结构
- 原有日志风格

禁止：

- 强制统一代码风格
- 自动大规模整理 import
- 自动重构目录结构

---

## 3.4 禁止主动升级依赖

禁止主动：

- pip install 最新版
- 修改 requirements
- 修改 Python 主版本
- 修改 Node 主版本
- 升级核心依赖

除非明确要求。

---

# 4. Python 与通用后端规范

## 4.1 强制 Type Hint

所有新增或重构函数：

必须声明：

- 参数类型
- 返回值类型

示例：

```python
def get_data(
    data: pd.DataFrame,
    threshold: float
) -> list[str]:
```

---

## 4.2 防御性编程

涉及：

- 数据处理
- API 输入
- 配置读取
- 文件解析
- 模型输入

时：

必须进行：

- 类型检查
- 空值检查
- 边界检查
- None 检查

避免：

```python
'<' not supported between instances of str and float
```

等运行时错误。

---

## 4.3 异常处理规范

关键逻辑必须：

```python
try:
    ...
except Exception:
    logger.exception(...)
```

禁止：

```python
except:
    pass
```

---

## 4.4 DataFrame / 数据结构操作必须安全

禁止：

- 隐式 inplace 修改
- 链式赋值风险
- 随意覆盖原对象

优先：

```python
df = df.copy()
```

---

# 5. 日志与调试规范

## 5.1 禁止 print

禁止：

```python
print()
```

必须使用：

```python
logger.info()
logger.warning()
logger.error()
logger.exception()
```

---

## 5.2 异常必须记录完整堆栈

必须：

```python
logger.exception()
```

或者：

```python
exc_info=True
```

---

## 5.3 日志必须包含关键参数

日志中必须包含：

- 输入参数
- 数据量
- 文件路径
- 算法名
- 关键状态

方便排查问题。

---

# 6. Docstring 与注释规范

## 6.1 所有复杂逻辑必须中文注释

包括：

- 数据清洗
- 特征工程
- SQL 逻辑
- Pipeline
- Tensor 变换
- 文件处理
- 并发逻辑

必须增加：

```python
# 中文行前注释
```

---

## 6.2 强制 Google 风格 Docstring

每个新函数：

必须包含：

```python
"""
函数功能说明.

Args:
    data: 输入数据.
    threshold: 阈值.

Returns:
    返回结果.
"""
```

---

# 7. 数据库与 SQL 安全规则

## 7.1 禁止危险 SQL

禁止生成：

```sql
DROP TABLE
TRUNCATE
DELETE 全表
ALTER 高风险结构
```

除非明确要求。

---

## 7.2 SQL 必须保持兼容性

生成 SQL 时：

- 必须兼容当前数据库
- 不允许混用方言
- 不允许生成危险 migration

---

## 7.3 禁止自动修改生产表结构

禁止：

- 自动 migration
- 自动 schema 修改
- 自动删除字段

---

# 8. OpenCode / AI Agent 行为规范

## 8.1 修改前必须先分析

修改代码前：

必须先：

```text
分析问题原因
分析调用链
分析上下游影响
```

然后再修改。

---

## 8.2 禁止只看片段代码直接修改

必须：

- 先理解上下文
- 先理解调用关系
- 再修改

---

## 8.3 输出必须完整

输出：

- 完整代码
- 完整函数
- 完整 import

禁止：

```python
# 其它代码保持不变
```

---

## 8.4 优先兼容旧逻辑

AI 的首要目标：

不是：

```text
写出最漂亮代码
```

而是：

```text
不破坏现有系统
```

---

# 9. Debug 规则

## 9.1 Debug 必须分析根因

禁止：

```text
盲目尝试修改
```

必须：

```text
先定位根因
再修复
```

---

## 9.2 Debug 必须结合日志

必须分析：

- logger
- traceback
- 调用链
- 参数流转

---

## 9.3 修复必须解释原因

每次修复：

必须说明：

```text
为什么出错
为什么这样修复
可能影响哪些地方
```

---

# 10. 输出规范

## 10.1 代码必须可直接运行

输出代码必须：

- 可直接运行
- 不缺 import
- 不缺依赖
- 不缺上下文

---

## 10.2 修改代码必须带上下文

修改代码时：

必须：

- 给出完整替换代码
- 给出替换位置
- 给出修改原因

---

## 10.3 不允许只输出理论

如果用户要求：

- 修复代码
- 修改代码
- Debug
- 重构

则必须：

- 输出完整代码
- 输出可执行方案

禁止只讲理论。

---

# 11. 最终核心原则（最高优先级）

始终牢记：

```text
稳定性 > 炫技
兼容性 > 重构
修复问题 > 代码美化
生产安全 > 理论最佳实践
```

你的首要目标：

是：

```text
安全地修复问题
```

而不是：

```text
重写整个系统
```

---

# 12. 自定义代码隔离规范（MinerU 项目专用）

## 12.1 自定义代码必须放在 custom 目录

本项目是基于开源 MinerU 的定制版本。

所有团队自定义的新增功能、函数、类，必须放在：

```text
mineru/utils/custom/
```

禁止直接修改上游源文件添加新函数。

## 12.2 对上游文件的修改仅限于 Hook 点

如果自定义功能需要在上游文件中触发，只能：

- 在上游文件中添加最小化的 try/except import hook
- import 路径必须指向 `mineru.utils.custom.*`
- hook 前后必须用 `[自定义]` 注释标记

示例：

```python
# [自定义] 生成旋转修正后的 PDF
# 合并上游时注意：此 hook 只依赖 mineru/utils/custom/ 下的自定义模块
try:
    from mineru.utils.custom.pdf_utils import generate_rotation_corrected_pdf
    ...
except Exception as exc:
    logger.warning(...)
```

## 12.3 合并上游时的优势

因为：
- 上游源文件只有最小化的 hook 改动
- 所有自定义逻辑在 `mineru/utils/custom/` 中（上游没有此目录，永不冲突）

所以合并上游更新时冲突极少。

## 12.4 新增自定义功能的推荐流程

1. 在 `mineru/utils/custom/` 下新建或扩展模块
2. 在上游文件中找到合适的 Hook 插入点
3. 用 `[自定义]` 标记包裹 hook 代码
4. 提交时 custom 模块代码和 hook 代码一起提交

## 12.5 当前 custom 模块结构

```text
mineru/utils/custom/
├── __init__.py       ← 模块说明
└── pdf_utils.py      ← PDF 旋转修正功能
```

